import React from 'react'

const Footer = ({ title }) => {
    return (
        <header class='footer'>
            <h3 style={{
                color: 'inherit',
            }}>{title}</h3>
        </header>
    )
}

export default Footer
